apiVersion: v1
kind: Service
metadata:
  name: {{ include "hellroll.fullname" . }}
  labels:
    {{- include "hellroll.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hellroll.selectorLabels" . | nindent 4 }}
